import { LimitCharacterDirective } from '../limit-character.directive';

describe('LimitCharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitCharacterDirective();
    expect(directive).toBeTruthy();
  });
});
